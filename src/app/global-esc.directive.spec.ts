import { GlobalEscDirective } from './global-esc.directive';

describe('GlobalEscDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalEscDirective();
    expect(directive).toBeTruthy();
  });
});
